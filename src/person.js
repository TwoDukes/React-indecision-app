
export const isAdult = age => age >= 18 ? "is an adult" : "is not an adult";

export const canDrink = age => age >= 21 ? "can drink" : "cannot drink";

export default age =>  age >= 65 ? "Is a Senior" : "Is not a Senior";